# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Internal audit / working docs (not published), but keep the README guide
# tracked on GitHub (it documents how to author this package's README).
/docs/*
!/docs/README_STRUCTURE_GUIDE.md

# AI assistant instructions: local only, not tracked or published.
/CLAUDE.md

# Reference clones of other packages for comparison. Local only.
/reference/

# Local hardware test harness (ESP firmware, scratch test apps), not published
/local_test/
example/integration_test/esp32_live_test.dart

# Flutter/Dart/Pub related
# Libraries should not include pubspec.lock, per https://dart.dev/guides/libraries/private-files#pubspeclock.
/pubspec.lock
**/doc/api/
.dart_tool/
.flutter-plugins-dependencies
/build/
/coverage/

# Internal implementation plan (local only, not published).
/doc/plan.md

# Firebase Hosting deploy cache.
.firebase/
